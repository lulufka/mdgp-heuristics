[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mdgp"
version = "0.1.0"
dependencies = [
    "networkx",
    "pandas",
    "leidenalg",
    "igraph",
    "numpy"
]

[tool.setuptools.packages.find]
where = ["src"]